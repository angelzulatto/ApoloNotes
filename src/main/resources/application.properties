# Nombre de la aplicación
spring.application.name=ApoloNotes

# Configuración de la base de datos PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/apolonotes
spring.datasource.username=postgres
spring.datasource.password=monti93
spring.datasource.driver-class-name=org.postgresql.Driver
server.servlet.context-path=/apolonotes


# Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuración general de Spring Boot
spring.main.banner-mode=off

spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=*
spring.web.cors.allowed-headers=*
